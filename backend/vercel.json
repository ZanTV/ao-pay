{
  "version": 2,
  "installCommand": "cd .. && npm install",
  "buildCommand": "cd .. && npm run vercel-build --workspace=backend",
  "functions": {
    "api/index.ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
